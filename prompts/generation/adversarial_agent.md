# Placeholder: Adversarial / Negative Agent

This prompt is reserved for future invalid-input, authorization, and robustness scenario
generation. Its output will be converted into pytest + `requests` tests by the Test Writer Agent.
It is not wired into the current planning orchestrator.
