# Placeholder: Happy-Path Agent

This prompt is reserved for the future test-generation phase. It will design valid request and
workflow scenarios for the Test Writer Agent, which will emit pytest tests using `requests`. It is
not wired into the current planning orchestrator.
